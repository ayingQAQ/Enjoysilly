# Release 文件说明

这个目录放面向普通使用者的启动脚本。

## Windows

双击运行：

```text
windows-install-and-run.bat
```

脚本会自动：

1. 检查 Node.js 和 npm。
2. 安装项目依赖。
3. 启动本地开发服务器。
4. 打开 `http://127.0.0.1:5173/`。

如果提示没有找到 Node.js，请先安装 Node.js LTS：

```text
https://nodejs.org/
```

关闭脚本窗口会停止本地服务。
